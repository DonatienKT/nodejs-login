pipeline{
    agent any

    stages{
        stage('dep install'){
            agent {docker {image 'node:9.8.0'
                args '-u root:sudo'}}
            steps{
             //   sh 'sudo chown -R 995:993 "/.npm"'
                sh 'npm install'
            }
        }
    }
}